spring:
  application:
    name: batch-job

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 200MB

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/batchjob}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${PORT:8080}

google:
  credentials: ${GOOGLE_CREDENTIALS:}
  drive:
    folder-id: ${GOOGLE_DRIVE_FOLDER_ID:}

logging:
  level:
    com.batchjob: INFO
